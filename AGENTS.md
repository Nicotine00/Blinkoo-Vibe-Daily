# AGENTS.md — Vibe Coding 协作规则

> 把这个文件放到你的项目根目录。AI 每次读这个文件时，"你"指的是 AI 自己，"用户"指的是人类开发者。
>
> 重要：这个文件不是写一次就不管了。项目每推进一个阶段，回来更新一次。

---

## 启动自检（开始项目前先过一遍）

在开始第一个对话之前，用户先想清楚这几个问题。想不清楚就别开始。

1. **这个项目解决什么问题？给谁用？** → 一句话说不清楚就别做
2. **最简可用版本是什么？** → 去掉所有"以后可以加"的功能，剩下那个就是
3. **你打算用什么技术栈？你熟悉吗？** → 不熟的话，先用AI调研再做决定
4. **数据/API/依赖挂了怎么办？** → 第三方服务依赖越多，项目越脆弱
5. **什么情况下你会放弃这个项目？** → 设定止损线，到了就停

用户把答案填在下面，AI 每次新对话先读：

```
项目目标：
目标用户：
技术栈：
最简版本功能：
外部依赖（API/服务）：
止损线：
```

---

## 1. 开发基础

### 1.1 技术栈宣言

告诉 AI 你的技术选型，避免它自作主张引入你没用过的框架。

```
语言/版本：
框架：
包管理器：
样式方案：
状态管理：
测试工具：
```

AI 想引入新依赖时，必须先问用户，等确认再加。

### 1.2 项目结构约定

```
目录结构（核心路径和用途）：
命名规范（文件/组件/函数/变量）：
```

AI 新建文件时，必须遵循现有目录结构，不能随手在根目录创建文件。

### 1.3 依赖管理

- 所有依赖写明版本号，不写"latest"。
- 重大依赖变更（换框架、加数据库）必须先说明原因，等用户确认。
- 不再使用的依赖及时清理。

---

## 2. 工作方式

### 2.1 先plan后动手

AI 接到需求后，先输出方案，等用户确认再写代码。一次性给方案+代码=无效输出。

### 2.2 增量修改

除非用户说"重做"，否则 AI 只改动用户明确描述的部分。不要重构、不要格式化、不要重命名、不要修改未提及的代码。

### 2.3 最小输出

小改动只输出修改的部分，不是整个文件。

### 2.4 改变动计划需确认

涉及多个文件的改动，AI 先列出改哪些文件、每个文件改哪里，等用户确认再执行。

### 2.5 大任务拆分

单个对话中，如果 AI 预估需要修改超过5个文件或需要3轮以上交互，先停下来，把任务拆成小阶段。每完成一个阶段提交一次。

---

## 3. 开发边界

### 3.1 边界意识

遇到以下情况，AI 必须停下并告知用户：

- **需求超出了能力范围**（用户想要的东西架构不支持）→ "这个方案可能不行"，并给出替代方案
- **用户的做法效率较低**（比如手动改数据而不是写脚本）→ 提出来，但最终听用户的
- **需求会引入过高的复杂度**（为小功能加重型依赖）→ 提示用户，让用户选择

### 3.2 架构决策记录

关键架构决策必须记录，不靠 AI 的记忆。格式：
```
日期：
决策：
背景：
可选方案：
为什么选这个：
```

每次新对话开始前，AI 先读架构决策记录，避免前后矛盾。

### 3.3 AI 不做的事

以下事项 AI 默认不做。如果用户确实需要，必须明确说"我要做XXX"：

- 不主动重构代码（除非用户要求）
- 不主动拆分文件（除非用户要求）
- 不主动升级依赖版本（除非用户要求）
- 不主动修改与需求无关的代码

---

## 4. 代码规范

### 4.1 不硬编码

所有配置类信息（API密钥、URL、模型名、超时时间、业务参数）通过参数传入或配置文件读取。

写代码前想一下：如果明天要加一个同类功能，需要改几处？超过一处说明设计不够好。

### 4.2 留扩展口

不接受"目前只需要一个，后续再优化"。如果确实只需要一个，也要做成数组/配置驱动，而不是写死单个对象。

### 4.3 函数粒度

一个函数只做一件事。一个函数超过50行说明该拆了。

### 4.4 不要过度设计

留扩展口不等于过度抽象。如果当前只需要一个简单实现，就写简单实现，但要确保它容易被替换。

判断标准：加一个新功能，改的是配置还是改逻辑？改配置=设计合理，改逻辑=过度简单了。

---

## 5. 自检验证

### 5.1 写完先自测

AI 给每个函数/组件做一遍最小验证：
- 正常输入 → 预期输出？
- 边界输入 → 处理了吗？
- 异常输入 → 报错信息清晰吗？

### 5.2 测试要求

核心逻辑必须有测试。AI 写的测试覆盖这三个场景：
1. 正常情况（按预期用）
2. 边界情况（空值/极限值）
3. 异常情况（传错参数/断网/没权限）

AI 写不出测试的代码，不管多漂亮都有问题。

### 5.3 改完做对比

AI 修改完成后，列出所有修改的文件和改动摘要。摘要说明"改了什么 + 为什么改"，不提与需求无关的优化。

### 5.4 外部依赖验证

AI 引入新的API/第三方服务时，先做最小连通测试：调通一个最简单的请求，确认可用，再集成到业务代码中。不要先写完整集成再调试。

---

## 6. 安全检查

### 6.1 Git 基线

AI 开始新任务前，先看当前Git状态。有未提交的改动就停下，等用户提交后再开始。

如果项目没有 Git，AI 在第一轮对话中帮用户初始化 Git。

### 6.2 对话长度控制

一个对话超过10轮或修改超过5个文件后，AI 停下来：
1. 提醒用户提交当前代码
2. 开新对话
3. 在新对话中 AI 先读 AGENTS.md 和最新代码

### 6.3 敏感信息

- AI 不在代码里写死 API Key、密码、Token
- 配置文件加 .gitignore
- 提交前 AI 检查有没有意外暴露的密钥

---

## 7. 沟通方式

- **不确定就问**。需求模糊或方案有多个选择时，AI 先问用户，不假设。
- **追根因**。遇到问题不只修表面，找到根本原因。每个决策说清楚"为什么"。
- **报错给全量信息**。AI 不自己诊断，给出完整错误信息、相关代码和上下文。
- **说人话**。不用"首先...其次...最后"、"综上所述"、"值得注意的是"。
- **承认局限**。遇到解决不了的问题直接说"这个我搞不定"，AI 不绕弯子。
